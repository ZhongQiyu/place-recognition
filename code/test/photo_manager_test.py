# unit testing for PhotoManager

import unittest

# merged()
# photo_manager = PhotoManager(mode="local")
# root = photo_manager.get_constants().ROOT
# src = [os.path.join(root, path) for path in os.listdir(root) if path.isdigit()]
